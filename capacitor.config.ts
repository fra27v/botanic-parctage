import { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: "com.fva.botanic-parctage",
  appName: "Botanic Parctage",
  bundledWebRuntime: false,
  npmClient: "npm",
  webDir: "www",
};

export default config;
